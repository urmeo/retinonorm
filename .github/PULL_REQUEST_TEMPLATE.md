## Summary

What this change does and why.

## Checklist

- [ ] `python -m ruff format --check .` and `python -m ruff check .` are clean
- [ ] `python -m mypy` is clean
- [ ] `python -m pytest -q --cov` passes on Python 3.9 and 3.12
- [ ] `python scripts/generate_validation_report.py --check` passes
- [ ] If a validation number moved: the regenerated report is committed and the change says which number moved and why

## Notes

Anything reviewers should look at closely.
